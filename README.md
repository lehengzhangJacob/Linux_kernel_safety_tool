# 操作系统设计赛道 - Linux 内核静态分析器# 操作系统设计赛道 - Linux 内核静态分析器设计文档# 操作系统设计赛道 - Linux 内核静态分析器设计文档# 操作系统设计赛道 - Linux 内核静态分析器# Linux 内核静态分析器 - 比赛提交文档# 操作系统比赛提交文档 - Linux 内核静态分析器# Linux 内核静态分析器 (GCC 插件)



## 1. 目标描述

本项目旨在构建一个针对 Linux 内核 (v6.6.1) 的**高精度静态分析与可视化系统**。

核心目标如下：## 1. 目标描述

1.  **语义级静态分析**：开发 GCC 插件，深入编译器中间表示（GIMPLE）层，实现对变量作用域的精准识别（解决 Shadowing 问题）和函数调用关系的精确提取。

2.  **并发安全检测**：基于静态锁集分析（Lockset Analysis）理论，实现过程间（Inter-procedural）的锁状态追踪，自动检测未受保护的全局变量访问，识别潜在竞态条件（Race Condition）。本项目旨在构建一个**高精度、可视化的 Linux 内核静态分析系统**。针对操作系统内核代码量大、并发逻辑复杂的特点，项目确立了以下核心目标：

3.  **图谱化审计辅助**：打通“源码 -> 分析数据 -> 图数据库”的工具链，将数百万行的内核代码转化为可交互的知识图谱（Neo4j），为内核开发者提供直观的代码审计视图。

1.  **语义级静态分析**：开发 GCC 插件，深入编译器中间表示（GIMPLE）层，实现对变量作用域的精准识别（解决 Shadowing 问题）和函数调用关系的精确提取。## 1. 目标描述

## 2. 比赛题目分析和相关资料调研

### 题目分析2.  **并发安全检测**：基于静态锁集分析（Lockset Analysis）理论，实现过程间（Inter-procedural）的锁状态追踪，自动检测未受保护的全局变量访问，识别潜在竞态条件（Race Condition）。

操作系统内核的并发安全性检测是系统软件领域的经典难题。

*   **规模挑战**：Linux 内核代码量巨大（千万行级），传统的全路径搜索算法面临状态爆炸问题。3.  **图谱化审计辅助**：打通“源码 -> 分析数据 -> 图数据库”的工具链，将数百万行的内核代码转化为可交互的知识图谱（Neo4j），为内核开发者提供直观的代码审计视图。本项目旨在构建一个**高精度、可视化的 Linux 内核静态分析系统**。针对操作系统内核代码量大、并发逻辑复杂的特点，项目确立了以下核心目标：

*   **语义挑战**：锁的获取与释放往往跨越多个函数调用，简单的词法分析（如 grep/regex）无法理解上下文语义。

*   **构建挑战**：内核构建系统（Kbuild）极其复杂，外部工具难以正确解析所有的宏定义和编译选项。



### 资料调研## 2. 比赛题目分析和相关资料调研1.  **语义级静态分析**：开发 GCC 插件，深入编译器中间表示（GIMPLE）层，实现对变量作用域的精准识别（解决 Shadowing 问题）和函数调用关系的精确提取。## 1. 目标描述

*   **静态分析技术**：

    *   *文本/AST 分析 (Cppcheck)*：速度快但精度低，无法处理指针别名。

    *   *LLVM/Clang*：生态丰富，但需要替换内核构建链，门槛较高。

    *   *GCC Plugin (本方案)*：**原生支持**。直接挂载于内核标准编译管线，零成本获取精准的类型信息和控制流图（CFG），适合内核级分析。### 2.1 题目难点分析2.  **并发安全检测**：基于静态锁集分析（Lockset Analysis）理论，实现过程间（Inter-procedural）的锁状态追踪，自动检测未受保护的全局变量访问，识别潜在竞态条件（Race Condition）。

*   **可视化方案**：

    *   *静态图 (Graphviz)*：无法承载内核级（10w+ 节点）数据。操作系统内核的并发安全性检测是工业界和学术界的公认难题，主要挑战在于：

    *   *图数据库 (Neo4j)*：**高性能**。支持亿级节点存储，提供 Cypher 查询语言，能够高效执行复杂图算法。

*   **规模效应**：Linux 内核包含千万行级代码，传统的全路径搜索算法面临状态爆炸问题。3.  **图谱化审计辅助**：打通“源码 -> 分析数据 -> 图数据库”的工具链，将数百万行的内核代码转化为可交互的知识图谱（Neo4j），为内核开发者提供直观的代码审计视图。本项目旨在开发一个针对 Linux 内核 (v6.6.1) 的 **基于 GCC 插件的静态分析器**。

## 3. 系统框架设计

系统采用**松耦合的流水线架构**，分为三个独立子系统：*   **上下文敏感性**：锁的获取与释放往往跨越多个函数调用，简单的词法分析（如 grep）无法理解上下文。



### 3.1 前端分析器 (GCC Plugin)*   **构建复杂性**：内核构建系统（Kbuild）极其复杂，外部工具难以正确解析所有的宏定义和编译选项。

*   **位置**：嵌入 GCC 编译过程 (`src/plugin/analyzer_plugin.cpp`)。

*   **功能**：

    *   **变量消歧**：利用 `TREE_STATIC` 属性精准识别全局变量。

    *   **锁集传播**：遍历 CFG，维护 `HeldLocks` 集合，追踪 `spin_lock`/`spin_unlock`。### 2.2 技术路线调研与选型## 2. 比赛题目分析和相关资料调研主要目标包括：## 1. 目标描述

    *   **摘要生成**：为每个函数生成锁副作用摘要，支持过程间分析。

*   **静态分析工具对比**：

### 3.2 中间处理层 (ETL Middleware)

*   **位置**：Python 数据处理脚本 (`tools/export_to_neo4j.py`)。    *   *基于文本/AST（如 Cppcheck）*：速度快但精度低，无法处理复杂的指针别名和控制流。

*   **功能**：

    *   **数据清洗**：处理多编译单元导致的重复符号定义。    *   *基于 LLVM/Clang*：生态丰富，但 Linux 内核对 Clang 的支持尚不如 GCC 成熟，且需改变标准构建流程。

    *   **实体建模**：将分析结果映射为图数据库的节点（Function, GlobalVariable）和边（CALLS, ACCESS）。

    *   *基于 GCC Plugin（本项目方案）*：**原生支持**。直接挂载于内核标准编译管线，零成本获取精准的类型信息和控制流图（CFG），适合内核级分析。### 2.1 题目难点分析1.  **精准语义分析**：利用 GCC 内部的 GIMPLE 表示，准确区分全局变量与局部变量，解决传统文本分析工具（如 grep, cscope）无法处理的变量遮蔽（Shadowing）问题。

### 3.3 后端存储与可视化 (Storage & View)

*   **位置**：Neo4j 图数据库。*   **可视化方案对比**：

*   **功能**：持久化存储分析结果，提供 Cypher 查询接口，支持复杂路径检索。

    *   *静态图（Graphviz/Gephi）*：无法承载内核级（10w+ 节点）数据，渲染卡顿，缺乏查询能力。操作系统内核的并发安全性检测是工业界和学术界的公认难题，主要挑战在于：

## 4. 开发计划

1.  **环境搭建与原型验证**：    *   *图数据库（Neo4j - 本项目方案）*：**高性能**。支持亿级节点存储，提供 Cypher 查询语言，能够高效执行“查找所有未加锁路径”等复杂图算法。

    *   配置 Linux 6.6.1 构建环境。

    *   开发最小化 GCC 插件，验证编译回调机制。*   **规模效应**：Linux 内核包含千万行级代码，传统的全路径搜索算法面临状态爆炸问题。2.  **并发竞争检测**：实现静态锁集分析（Lockset Analysis）算法，通过过程间分析（Inter-procedural Analysis）检测未受锁保护的全局变量访问，发现潜在的竞态条件（Race Conditions）。本项目旨在开发一个针对 Linux 内核 (v6.6.1) 的 **基于 GCC 插件的静态分析器**。

2.  **核心分析逻辑开发**：

    *   实现 GIMPLE 语句遍历器。## 3. 系统框架设计

    *   开发全局变量精准识别逻辑。

    *   实现基础的锁集分析算法。*   **上下文敏感性**：锁的获取与释放往往跨越多个函数调用，简单的词法分析（如 grep）无法理解上下文。

3.  **数据流与可视化打通**：

    *   设计 JSON 中间交换格式。### 3.1 总体架构

    *   搭建 Neo4j 数据库，编写 Python 导入脚本。

4.  **系统集成与优化**：系统采用**松耦合的流水线架构**，分为三个独立子系统：*   **构建复杂性**：内核构建系统（Kbuild）极其复杂，外部工具难以正确解析所有的宏定义和编译选项。3.  **可视化展示**：构建从内核源码到图数据库（Neo4j）的完整数据流，将复杂的函数调用关系和变量访问行为转化为直观的图谱，辅助内核开发者进行代码审计。

    *   集成至 Kbuild 系统。

    *   编写自动化编排脚本 `full_run.sh`。1.  **前端分析器 (Compiler-side)**：嵌入 GCC 编译过程，负责“理解”代码。

    *   优化大规模分析时的内存占用。

2.  **中间处理层 (ETL Middleware)**：负责数据的清洗、融合与格式化。

## 5. 比赛过程中的重要进展

1.  **攻克内核构建集成**：成功将自定义 GCC 插件无缝嵌入 Linux Kbuild 系统，无需修改内核源码即可进行分析。3.  **后端存储与可视化 (Storage & View)**：负责数据的持久化存储与交互式查询。

2.  **实现过程间分析**：从最初只能分析单个函数内部的锁状态，进化到能够追踪跨函数调用的锁传递（如 `wrapper` 函数），显著降低了漏报率。

3.  **可视化性能突破**：放弃前端渲染方案，转向后端图数据库方案，成功实现了对完整内核模块调用关系的秒级查询。### 2.2 技术路线调研与选型主要目标包括：## 1. 目标描述## 项目概述



## 6. 系统测试情况### 3.2 详细模块设计

### 测试策略

采用“单元测试为主，集成测试为辅”的策略。*   **静态分析工具对比**：



### 测试结果#### 3.2.1 GCC 插件模块 (`src/plugin/analyzer_plugin.cpp`)

1.  **单元测试 (`test/viz_test.c`)**：

    *   成功识别全局变量读写。*   **介入时机**：注册为 `PASS_IPA_PTA` 后的 IPA Pass，确保在别名分析之后、机器码生成之前执行。    *   *基于文本/AST（如 Cppcheck）*：速度快但精度低，无法处理复杂的指针别名和控制流。## 2. 比赛题目分析和相关资料调研

    *   成功检测未加锁访问路径。

    *   验证了函数指针调用的追踪能力。*   **核心算法**：

2.  **集成测试 (Linux Kernel v6.6.1)**：

    *   **规模**：分析了数万个源文件。    *   **变量消歧**：利用 `TREE_STATIC` 和 `DECL_EXTERNAL` 属性，结合 `varpool_node`，实现全局变量的 100% 准确识别。    *   *基于 LLVM/Clang*：生态丰富，但 Linux 内核对 Clang 的支持尚不如 GCC 成熟，且需改变标准构建流程。

    *   **数据量**：生成节点数 > 80,000，边数 > 200,000。

    *   **性能**：全量分析耗时增加可控，Neo4j 查询响应迅速。    *   **锁集传播**：维护 `HeldLocks` 集合。遍历 CFG，遇到 `spin_lock` 添加锁，遇到 `spin_unlock` 移除锁。



## 7. 遇到的主要问题和解决方法    *   **摘要生成**：为每个函数生成 `Summary`（包含该函数对锁的副作用），供调用者复用，实现高效的过程间分析。    *   *基于 GCC Plugin（本项目方案）*：**原生支持**。直接挂载于内核标准编译管线，零成本获取精准的类型信息和控制流图（CFG），适合内核级分析。1.  **精准语义分析**：利用 GCC 内部的 GIMPLE 表示，准确区分全局变量与局部变量，解决传统文本分析工具（如 grep, cscope）无法处理的变量遮蔽（Shadowing）问题。



| 问题类别 | 问题描述 | 解决方案 |

| :--- | :--- | :--- |

| **准确性** | 局部变量与全局变量同名（Shadowing）导致误报。 | 放弃基于字符串名称的匹配，转而使用 GCC 内部的 `tree` 结构体属性（`TREE_STATIC`）进行语义级判断。 |#### 3.2.2 数据转换模块 (`tools/export_to_neo4j.py`)*   **可视化方案对比**：

| **性能** | 大规模图数据导致浏览器前端渲染崩溃。 | 架构重构。引入 Neo4j 图数据库，将计算压力转移至后端，前端仅按需加载子图。 |

| **易用性** | 评委复现环境配置繁琐（JDK/Neo4j 版本不兼容）。 | 实现“全包含”发布。将特定版本的 JDK 和 Neo4j 二进制包集成到源码库 `tools/` 目录，并编写脚本自动配置环境变量。 |*   **数据清洗**：处理多编译单元导致的重复符号定义。



## 8. 分工和协作*   **实体建模**：    *   *静态图（Graphviz/Gephi）*：无法承载内核级（10w+ 节点）数据，渲染卡顿，缺乏查询能力。### 2.1 题目分析

*   **张乐桁**：负责代码主要编码。

*   **潘利源**：负责修改代码。    *   `Node: Function` (属性: name, file, line)

*   **黄永康**：负责撰写文档。

    *   `Node: GlobalVariable` (属性: name, type)    *   *图数据库（Neo4j - 本项目方案）*：**高性能**。支持亿级节点存储，提供 Cypher 查询语言，能够高效执行“查找所有未加锁路径”等复杂图算法。

## 9. 提交仓库目录和文件描述

```text    *   `Edge: CALLS` (属性: count)

.

├── src/    *   `Edge: ACCESS` (属性: type=READ/WRITE, lock_status=PROTECTED/UNPROTECTED)操作系统内核的并发安全性是系统稳定性的基石。随着内核规模的增长，人工审计变得极其困难。2.  **并发竞争检测**：实现静态锁集分析（Lockset Analysis）算法，通过过程间分析（Inter-procedural Analysis）检测未受锁保护的全局变量访问，发现潜在的竞态条件（Race Conditions）。本项目旨在开发一个针对 Linux 内核 (v6.6.1) 的 **基于 GCC 插件的静态分析器**。本项目实现了一个针对 Linux 内核 (v6.6.1) 的 **基于 GCC 插件的静态分析器**。与传统的基于文本的解析器不同，该工具直接挂钩到 GCC 编译过程（IPA pass），以对源代码执行准确的语义分析和过程间分析。

│   └── plugin/analyzer_plugin.cpp    # [核心] GCC 静态分析插件源码

├── tools/

│   ├── export_to_neo4j.py            # [ETL] 数据清洗与导入脚本

│   ├── jdk-17.0.2/                   # [环境] 嵌入式 Java 运行时#### 3.2.3 自动化编排 (`full_run.sh`)## 3. 系统框架设计

│   └── neo4j-community-4.4.34/       # [环境] 嵌入式图数据库

├── linux-6.6.1/                      # [目标] 待分析的 Linux 内核源码*   **生命周期管理**：封装了 `make clean` -> `make modules` -> `python ETL` -> `neo4j-admin import` -> `neo4j start` 的全流程，屏蔽底层复杂性。

├── analysis_data/                    # [输出] 插件生成的原始 JSON 数据

├── neo4j_data_linux-6.6.1/           # [输出] 图数据库 CSV 导入文件*   **难点**：内核代码量巨大（千万行级），函数调用关系复杂，锁机制多样（Spinlock, Mutex, RCU等）。

├── full_run.sh                       # [脚本] 一键全自动化运行脚本

├── test_toolchain.sh                 # [脚本] 单元测试脚本## 4. 开发计划

├── start_neo4j.sh                    # [脚本] 数据库独立启动脚本

├── NEO4J_QUERIES.md                  # [文档] 图谱查询语句手册*   **阶段一：技术验证 (PoC)**### 3.1 总体架构

├── README.md                         # [文档] 项目主设计文档

└── USAGE.md                          # [文档] 用户操作指南    *   搭建 GCC 插件开发环境。

```

    *   实现最小化插件：打印所有编译的函数名。系统采用**松耦合的流水线架构**，分为三个独立子系统：*   **需求**：需要一种自动化的工具，能够在编译阶段提取语义信息，并能跨越函数边界追踪锁的状态。3.  **可视化展示**：构建从内核源码到图数据库（Neo4j）的完整数据流，将复杂的函数调用关系和变量访问行为转化为直观的图谱，辅助内核开发者进行代码审计。

## 10. 比赛收获

*   **编译器视角看代码**：跳出了传统的源码阅读视角，学会了从 IR（中间表示）和 CFG（控制流图）的维度去理解程序的执行语义。*   **阶段二：核心逻辑实现**

*   **工程化能力提升**：在处理 Linux 内核这样的大型工程时，深刻体会到了构建系统、依赖管理和自动化脚本的重要性。

*   **跨领域技术融合**：成功将编译技术（GCC Plugin）与数据库技术（Neo4j）相结合，探索了“代码即数据”的分析新范式。    *   实现 GIMPLE 语句遍历器 (Gimple Walker)。1.  **前端分析器 (Compiler-side)**：嵌入 GCC 编译过程，负责“理解”代码。


    *   开发全局变量识别逻辑。

    *   开发基础的锁操作识别 (Lock/Unlock)。2.  **中间处理层 (ETL Middleware)**：负责数据的清洗、融合与格式化。

*   **阶段三：数据流打通**

    *   定义 JSON 中间交换格式。3.  **后端存储与可视化 (Storage & View)**：负责数据的持久化存储与交互式查询。

    *   搭建 Neo4j 数据库，编写 Python 导入脚本。

*   **阶段四：系统集成与优化**### 2.2 资料调研主要目标包括：

    *   集成至 Linux 6.6.1 构建系统。

    *   优化内存占用，解决大规模分析时的 OOM 问题。### 3.2 详细模块设计

    *   编写自动化脚本 `full_run.sh`。

*   **静态分析技术**：调研了基于文本的匹配（准确率低）、基于 LLVM 的分析（需要改变构建链）和基于 GCC 插件的分析。最终选择 GCC 插件，因为 Linux 内核原生支持 GCC，且能直接利用现有的构建系统。

## 5. 比赛过程中的重要进展

1.  **攻克内核构建集成**：成功将自定义 GCC 插件无缝嵌入 Linux Kbuild 系统，无需修改内核源码即可进行分析。#### 3.2.1 GCC 插件模块 (`src/plugin/analyzer_plugin.cpp`)

2.  **实现过程间分析**：从最初只能分析单个函数内部的锁状态，进化到能够追踪跨函数调用的锁传递（如 `wrapper` 函数），显著降低了漏报率。

3.  **可视化性能突破**：放弃前端渲染方案，转向后端图数据库方案，成功实现了对完整内核模块调用关系的秒级查询。*   **介入时机**：注册为 `PASS_IPA_PTA` 后的 IPA Pass，确保在别名分析之后、机器码生成之前执行。*   **中间表示 (IR)**：深入研究了 GCC 的 GIMPLE 表示。GIMPLE 保留了高级语言的语义（如变量作用域），同时简化了控制流，非常适合进行静态分析。## 2. 比赛题目分析和相关资料调研



## 6. 系统测试情况*   **核心算法**：



### 6.1 测试策略    *   **变量消歧**：利用 `TREE_STATIC` 和 `DECL_EXTERNAL` 属性，结合 `varpool_node`，实现全局变量的 100% 准确识别。*   **图数据库**：调研了 Gephi（适合静态图，处理大规模数据性能较差）和 Neo4j（原生图数据库，支持复杂查询）。最终选择 Neo4j 以支持内核级别的海量节点（80k+ 函数节点）。

采用“单元测试为主，集成测试为辅”的策略。

*   **单元测试 (`test/`)**：构造包含各类边界情况（如指针别名、死锁模式、复杂控制流）的 C 代码片段，验证插件逻辑的正确性。    *   **锁集传播**：维护 `HeldLocks` 集合。遍历 CFG，遇到 `spin_lock` 添加锁，遇到 `spin_unlock` 移除锁。

*   **集成测试 (Linux Kernel)**：在真实内核环境运行，验证系统的鲁棒性和性能。

    *   **摘要生成**：为每个函数生成 `Summary`（包含该函数对锁的副作用），供调用者复用，实现高效的过程间分析。### 题目分析1.  **精准语义分析**：利用 GCC 内部的 GIMPLE 表示，准确区分全局变量与局部变量，解决传统文本分析工具（如 grep, cscope）无法处理的变量遮蔽（Shadowing）问题。## 核心功能

### 6.2 测试结果

*   **功能性验证**：在 `test/viz_test.c` 中，系统成功识别出所有预埋的全局变量读写操作和未加锁访问。

*   **性能验证**：分析完整内核模块耗时增加不超过 30%，生成的图谱包含 80,000+ 节点和 200,000+ 边，Neo4j 查询响应时间 < 100ms。

#### 3.2.2 数据转换模块 (`tools/export_to_neo4j.py`)## 3. 系统框架设计

## 7. 遇到的主要问题和解决方法

*   **数据清洗**：处理多编译单元导致的重复符号定义。

| 问题类别 | 问题描述 | 解决方案 |

| :--- | :--- | :--- |*   **实体建模**：操作系统内核的并发安全性是系统稳定性的基石。随着内核规模的增长，人工审计变得极其困难。

| **准确性** | 局部变量与全局变量同名（Shadowing）导致误报。 | 放弃基于字符串名称的匹配，转而使用 GCC 内部的 `tree` 结构体属性（`TREE_STATIC`）进行语义级判断。 |

| **性能** | 大规模图数据导致浏览器前端渲染崩溃。 | 架构重构。引入 Neo4j 图数据库，将计算压力转移至后端，前端仅按需加载子图。 |    *   `Node: Function` (属性: name, file, line)

| **易用性** | 评委复现环境配置繁琐（JDK/Neo4j 版本不兼容）。 | 实现“全包含”发布。将特定版本的 JDK 和 Neo4j 二进制包集成到源码库 `tools/` 目录，并编写脚本自动配置环境变量。 |

    *   `Node: GlobalVariable` (属性: name, type)### 3.1 整体架构

## 8. 分工和协作

*   **张乐桁**：负责代码主要编码。    *   `Edge: CALLS` (属性: count)

*   **潘利源**：负责修改代码。

*   **黄永康**：负责撰写文档。    *   `Edge: ACCESS` (属性: type=READ/WRITE, lock_status=PROTECTED/UNPROTECTED)系统采用“编译时分析 + 离线处理 + 在线查询”的三层架构：*   **难点**：内核代码量巨大（千万行级），函数调用关系复杂，锁机制多样（Spinlock, Mutex, RCU等）。2.  **并发竞争检测**：实现静态锁集分析（Lockset Analysis）算法，通过过程间分析（Inter-procedural Analysis）检测未受锁保护的全局变量访问，发现潜在的竞态条件（Race Conditions）。



## 9. 提交仓库目录和文件描述

```text

.#### 3.2.3 自动化编排 (`full_run.sh`)1.  **数据提取层 (GCC Plugin)**：挂载于 GCC 编译管线，提取控制流和数据流信息。

├── src/

│   └── plugin/analyzer_plugin.cpp    # [核心] GCC 静态分析插件源码*   **生命周期管理**：封装了 `make clean` -> `make modules` -> `python ETL` -> `neo4j-admin import` -> `neo4j start` 的全流程，屏蔽底层复杂性。

├── tools/

│   ├── export_to_neo4j.py            # [ETL] 数据清洗与导入脚本2.  **数据处理层 (Python ETL)**：清洗、去重、格式化分析数据。*   **需求**：需要一种自动化的工具，能够在编译阶段提取语义信息，并能跨越函数边界追踪锁的状态。

│   ├── jdk-17.0.2/                   # [环境] 嵌入式 Java 运行时

│   └── neo4j-community-4.4.34/       # [环境] 嵌入式图数据库## 4. 开发计划

├── linux-6.6.1/                      # [目标] 待分析的 Linux 内核源码

├── analysis_data/                    # [输出] 插件生成的原始 JSON 数据*   **阶段一：技术验证 (PoC)**3.  **数据展示层 (Neo4j)**：存储图谱数据，提供 Cypher 查询接口。

├── neo4j_data_linux-6.6.1/           # [输出] 图数据库 CSV 导入文件

├── full_run.sh                       # [脚本] 一键全自动化运行脚本    *   搭建 GCC 插件开发环境。

├── test_toolchain.sh                 # [脚本] 单元测试脚本

├── start_neo4j.sh                    # [脚本] 数据库独立启动脚本    *   实现最小化插件：打印所有编译的函数名。3.  **可视化展示**：构建从内核源码到图数据库（Neo4j）的完整数据流，将复杂的函数调用关系和变量访问行为转化为直观的图谱，辅助内核开发者进行代码审计。### 1. 准确的变量识别

├── NEO4J_QUERIES.md                  # [文档] 图谱查询语句手册

├── README.md                         # [文档] 项目主设计文档*   **阶段二：核心逻辑实现**

└── USAGE.md                          # [文档] 用户操作指南

```    *   实现 GIMPLE 语句遍历器 (Gimple Walker)。### 3.2 模块设计



## 10. 比赛收获    *   开发全局变量识别逻辑。

*   **编译器视角看代码**：跳出了传统的源码阅读视角，学会了从 IR（中间表示）和 CFG（控制流图）的维度去理解程序的执行语义。

*   **工程化能力提升**：在处理 Linux 内核这样的大型工程时，深刻体会到了构建系统、依赖管理和自动化脚本的重要性。    *   开发基础的锁操作识别 (Lock/Unlock)。### 资料调研

*   **跨领域技术融合**：成功将编译技术（GCC Plugin）与数据库技术（Neo4j）相结合，探索了“代码即数据”的分析新范式。

*   **阶段三：数据流打通**

    *   定义 JSON 中间交换格式。#### 3.2.1 GCC 插件模块 (`src/plugin/analyzer_plugin.cpp`)

    *   搭建 Neo4j 数据库，编写 Python 导入脚本。

*   **阶段四：系统集成与优化***   **功能**：作为 IPA (Inter-procedural Analysis) Pass 运行。*   **静态分析技术**：调研了基于文本的匹配（准确率低）、基于 LLVM 的分析（需要改变构建链）和基于 GCC 插件的分析。最终选择 GCC 插件，因为 Linux 内核原生支持 GCC，且能直接利用现有的构建系统。*   利用 GCC 内部的 GIMPLE 表示，以 100% 的准确率区分 **全局** 和 **局部** 变量。

    *   集成至 Linux 6.6.1 构建系统。

    *   优化内存占用，解决大规模分析时的 OOM 问题。*   **核心类**：

    *   编写自动化脚本 `full_run.sh`。

    *   `FunctionAnalysis`：存储单个函数的分析结果（调用集、读写集）。*   **中间表示 (IR)**：深入研究了 GCC 的 GIMPLE 表示。GIMPLE 保留了高级语言的语义（如变量作用域），同时简化了控制流，非常适合进行静态分析。

## 5. 比赛过程中的重要进展

1.  **攻克内核构建集成**：成功将自定义 GCC 插件无缝嵌入 Linux Kbuild 系统，无需修改内核源码即可进行分析。    *   `lock_effect_cache`：缓存函数的锁副作用（+1 加锁, -1 解锁），用于加速递归分析。

2.  **实现过程间分析**：从最初只能分析单个函数内部的锁状态，进化到能够追踪跨函数调用的锁传递（如 `wrapper` 函数），显著降低了漏报率。

3.  **可视化性能突破**：放弃前端渲染方案，转向后端图数据库方案，成功实现了对完整内核模块调用关系的秒级查询。*   **关键算法**：*   **图数据库**：调研了 Gephi（适合静态图，处理大规模数据性能较差）和 Neo4j（原生图数据库，支持复杂查询）。最终选择 Neo4j 以支持内核级别的海量节点（80k+ 函数节点）。## 2. 比赛题目分析和相关资料调研*   解决了局部变量遮蔽全局变量时常见的误识别问题。



## 6. 系统测试情况    *   **GIMPLE 遍历**：使用 `gimple_stmt_iterator` 遍历所有基本块。



### 6.1 测试策略    *   **变量消歧**：通过检查 `TREE_STATIC` 和 `DECL_EXTERNAL` 标志区分全局变量。

采用“单元测试为主，集成测试为辅”的策略。

*   **单元测试 (`test/`)**：构造包含各类边界情况（如指针别名、死锁模式、复杂控制流）的 C 代码片段，验证插件逻辑的正确性。

*   **集成测试 (Linux Kernel)**：在真实内核环境运行，验证系统的鲁棒性和性能。

#### 3.2.2 数据处理模块 (`tools/export_to_neo4j.py`)## 3. 系统框架设计### 题目分析

### 6.2 测试结果

*   **功能性验证**：在 `test/viz_test.c` 中，系统成功识别出所有预埋的全局变量读写操作和未加锁访问。*   **功能**：将分散在各个编译单元的 JSON 文件合并为图数据库导入文件。

*   **性能验证**：分析完整内核模块耗时增加不超过 30%，生成的图谱包含 80,000+ 节点和 200,000+ 边，Neo4j 查询响应时间 < 100ms。

*   **实体映射**：系统整体架构分为三个主要阶段：

## 7. 遇到的主要问题和解决方法

    *   **节点 (Nodes)**：`Function` (函数), `GlobalVariable` (全局变量)。

| 问题类别 | 问题描述 | 解决方案 |

| :--- | :--- | :--- |    *   **边 (Edges)**：`CALLS` (调用), `READS` (读取), `WRITES` (写入)。操作系统内核的并发安全性是系统稳定性的基石。随着内核规模的增长，人工审计变得极其困难。### 2. 全面的用法检测

| **准确性** | 局部变量与全局变量同名（Shadowing）导致误报。 | 放弃基于字符串名称的匹配，转而使用 GCC 内部的 `tree` 结构体属性（`TREE_STATIC`）进行语义级判断。 |

| **性能** | 大规模图数据导致浏览器前端渲染崩溃。 | 架构重构。引入 Neo4j 图数据库，将计算压力转移至后端，前端仅按需加载子图。 |*   **去重策略**：使用 Python `set` 和 `dict` 消除重复的函数声明和调用关系。

| **易用性** | 评委复现环境配置繁琐（JDK/Neo4j 版本不兼容）。 | 实现“全包含”发布。将特定版本的 JDK 和 Neo4j 二进制包集成到源码库 `tools/` 目录，并编写脚本自动配置环境变量。 |

### 3.1 数据提取层 (GCC Plugin)

## 8. 分工和协作

*   **张乐桁**：负责代码主要编码。#### 3.2.3 自动化编排模块 (`full_run.sh`)

*   **潘利源**：负责修改代码。

*   **黄永康**：负责撰写文档。*   **功能**：串联整个工具链，实现一键式运行。*   **挂载点**：作为 GCC 的 IPA (Inter-procedural Analysis) Pass 挂载到编译流程中。*   **难点**：内核代码量巨大（千万行级），函数调用关系复杂，锁机制多样（Spinlock, Mutex, RCU等）。*   **全局变量写入**: 检测全局变量何时被修改。



## 9. 提交仓库目录和文件描述*   **流程**：`Clean` -> `Build Kernel (with Plugin)` -> `ETL` -> `Neo4j Import` -> `Start DB`。

```text

.*   **核心逻辑**：

├── src/

│   └── plugin/analyzer_plugin.cpp    # [核心] GCC 静态分析插件源码## 4. 开发计划

├── tools/

│   ├── export_to_neo4j.py            # [ETL] 数据清洗与导入脚本1.  **环境搭建阶段**：配置 Linux 6.6.1 内核构建环境，熟悉 GCC 插件开发接口。    *   **变量识别**：遍历 GIMPLE 语句，区分 Global/Local 变量。*   **需求**：需要一种自动化的工具，能够在编译阶段提取语义信息，并能跨越函数边界追踪锁的状态。*   **全局变量读取**: 检测全局变量何时被访问。

│   ├── jdk-17.0.2/                   # [环境] 嵌入式 Java 运行时

│   └── neo4j-community-4.4.34/       # [环境] 嵌入式图数据库2.  **原型验证阶段**：编写最小化插件，验证能否在内核编译过程中打印函数名和变量名。

├── linux-6.6.1/                      # [目标] 待分析的 Linux 内核源码

├── analysis_data/                    # [输出] 插件生成的原始 JSON 数据3.  **核心功能开发**：    *   **行为捕获**：记录 READ, WRITE, CALL, ASSIGN (函数指针赋值) 等行为。

├── neo4j_data_linux-6.6.1/           # [输出] 图数据库 CSV 导入文件

├── full_run.sh                       # [脚本] 一键全自动化运行脚本    *   实现 GIMPLE 语句遍历器。

├── test_toolchain.sh                 # [脚本] 单元测试脚本

├── start_neo4j.sh                    # [脚本] 数据库独立启动脚本    *   实现全局/局部变量区分逻辑。    *   **锁集分析**：维护当前路径持有的锁集合，检测未加锁的全局变量访问。*   **函数指针赋值**: 跟踪函数的地址何时被赋值给指针 (例如 `ptr = &func`)。

├── NEO4J_QUERIES.md                  # [文档] 图谱查询语句手册

├── README.md                         # [文档] 项目主设计文档    *   实现锁集分析算法（Lockset Analysis）。

└── USAGE.md                          # [文档] 用户操作指南

```4.  **数据流打通**：设计 JSON 输出格式，编写 Python 转换脚本，搭建 Neo4j 数据库。*   **输出**：为每个编译单元生成 JSON 格式的分析报告。



## 10. 比赛收获5.  **大规模测试与优化**：在完整内核上运行，解决内存溢出和性能瓶颈问题。

*   **编译器视角看代码**：跳出了传统的源码阅读视角，学会了从 IR（中间表示）和 CFG（控制流图）的维度去理解程序的执行语义。

*   **工程化能力提升**：在处理 Linux 内核这样的大型工程时，深刻体会到了构建系统、依赖管理和自动化脚本的重要性。6.  **文档与可视化**：编写查询语句文档，优化图谱展示效果。### 资料调研*   **函数指针调用**: 跟踪间接函数调用。

*   **跨领域技术融合**：成功将编译技术（GCC Plugin）与数据库技术（Neo4j）相结合，探索了“代码即数据”的分析新范式。



## 5. 比赛过程中的重要进展### 3.2 数据处理层 (Python ETL)

*   **突破变量遮蔽难题**：成功利用 GCC `varpool_node` 和 `tree` 结构，以 100% 准确率区分了同名局部变量和全局变量。

*   **实现过程间锁分析**：从最初的单函数分析，进化到支持跨函数调用的锁状态追踪，能够识别 `wrapper` 函数（如 `my_lock` 封装 `spin_lock`）的语义。*   **数据清洗**：合并分散的 JSON 文件，去除重复数据。*   **静态分析技术**：调研了基于文本的匹配（准确率低）、基于 LLVM 的分析（需要改变构建链）和基于 GCC 插件的分析。最终选择 GCC 插件，因为 Linux 内核原生支持 GCC，且能直接利用现有的构建系统。

*   **全自动化流程**：编写了 `full_run.sh`，实现了从“清理 -> 编译 -> 分析 -> 导入数据库 -> 启动服务”的一键式操作。

*   **格式转换**：将分析结果转换为图数据库所需的节点（Nodes）和边（Edges）格式（CSV）。

## 6. 系统测试情况

*   **实体映射**：*   **中间表示 (IR)**：深入研究了 GCC 的 GIMPLE 表示。GIMPLE 保留了高级语言的语义（如变量作用域），同时简化了控制流，非常适合进行静态分析。### 3. 并发竞争检测 (Race Condition Detection)

### 6.1 单元测试

*   **测试脚本**：`test_toolchain.sh`    *   Node: Function, GlobalVariable, Lock

*   **测试用例**：`test/viz_test.c`

*   **测试内容**：    *   Edge: CALLS, READS, WRITES, ACQUIRES, RELEASES*   **图数据库**：调研了 Gephi（适合静态图，处理大规模数据性能较差）和 Neo4j（原生图数据库，支持复杂查询）。最终选择 Neo4j 以支持内核级别的海量节点（80k+ 函数节点）。*   **锁集分析 (Lockset Analysis)**: 

    *   验证全局变量读写是否被正确捕获。

    *   验证函数调用关系是否完整。

    *   验证 CSV 输出格式是否符合 Neo4j 导入标准。

### 3.3 数据展示层 (Neo4j)    *   实现了一个静态的锁集分析算法。

### 6.2 集成测试 (Linux Kernel v6.6.1)

*   **测试环境**：Ubuntu Linux, GCC 13, JDK 17, Neo4j 4.4.34。*   **存储**：使用 Neo4j 图数据库存储全量内核调用图。

*   **测试规模**：完整编译 Linux 6.6.1 内核，分析了数万个源文件。

*   **结果**：*   **查询**：通过 Cypher 查询语言提供灵活的检索（如“查找所有访问变量 X 且未加锁的路径”）。## 3. 系统框架设计    *   遍历调用图，动态维护当前路径持有的锁集合。

    *   生成节点数：> 80,000 (函数与变量)

    *   生成边数：> 200,000 (调用与访问关系)

    *   成功检测出内核中已知的并发访问模式。

## 4. 开发计划系统整体架构分为三个主要阶段：*   **过程间分析 (Inter-procedural Analysis)**:

## 7. 遇到的主要问题和解决方法

### 问题 1：局部变量与全局变量同名导致误报1.  **环境搭建阶段**：配置 Linux 6.6.1 内核构建环境，熟悉 GCC 插件开发接口。

*   **现象**：简单的字符串匹配会将局部变量 `count` 误认为是全局变量 `count`。

*   **解决**：放弃基于名称的匹配，转而检查 GCC 的 `TREE_STATIC` 和 `DECL_EXTERNAL` 标志，准确判断变量存储类型。2.  **原型验证阶段**：编写最小化插件，验证能否在内核编译过程中打印函数名和变量名。    *   支持跨函数锁状态追踪。



### 问题 2：大规模数据可视化崩溃3.  **核心功能开发**：

*   **现象**：最初尝试使用前端库（vis.js）和 Gephi 直接加载所有数据，导致浏览器崩溃或软件无响应。

*   **解决**：引入 Neo4j 图数据库。将数据存储在磁盘，按需查询（Cypher），只渲染用户关心的子图（Subgraph），解决了性能瓶颈。    *   实现 GIMPLE 语句遍历器。### 3.1 数据提取层 (GCC Plugin)    *   自动分析包装函数（Wrapper Functions，如 `my_lock()` 调用 `spin_lock()`) 的锁副作用。



### 问题 3：环境配置复杂    *   实现全局/局部变量区分逻辑。

*   **现象**：评委或协作者复现项目时，常因 JDK 版本、Neo4j 版本不一致导致运行失败。

*   **解决**：将 JDK 17 和 Neo4j Community 版直接集成到项目 `tools/` 目录中，并编写脚本自动配置环境变量，实现“开箱即用”。    *   实现锁集分析算法（Lockset Analysis）。*   **挂载点**：作为 GCC 的 IPA (Inter-procedural Analysis) Pass 挂载到编译流程中。    *   使用递归摘要生成（Recursive Summary Generation）和缓存机制，高效处理调用链。



## 8. 分工和协作4.  **数据流打通**：设计 JSON 输出格式，编写 Python 转换脚本，搭建 Neo4j 数据库。

*   **张乐桁**：负责代码主要编码。

*   **潘利源**：负责修改代码。5.  **大规模测试与优化**：在完整内核上运行，解决内存溢出和性能瓶颈问题。*   **核心逻辑**：*   **未保护访问警告**: 

*   **黄永康**：负责撰写文档。

6.  **文档与可视化**：编写查询语句文档，优化图谱展示效果。

## 9. 提交仓库目录和文件描述

```text    *   **变量识别**：遍历 GIMPLE 语句，区分 Global/Local 变量。    *   当全局变量在未持有锁（锁集为空）的情况下被访问（读/写）时，发出 `[RACE_WARNING]` 警告。

.

├── src/## 5. 比赛过程中的重要进展

│   └── plugin/analyzer_plugin.cpp    # GCC 插件核心源代码

├── tools/*   **突破变量遮蔽难题**：成功利用 GCC `varpool_node` 和 `tree` 结构，以 100% 准确率区分了同名局部变量和全局变量。    *   **行为捕获**：记录 READ, WRITE, CALL, ASSIGN (函数指针赋值) 等行为。

│   ├── export_to_neo4j.py            # 数据转换脚本 (JSON -> CSV)

│   ├── jdk-17.0.2/                   # 预置 Java 运行环境*   **实现过程间锁分析**：从最初的单函数分析，进化到支持跨函数调用的锁状态追踪，能够识别 `wrapper` 函数（如 `my_lock` 封装 `spin_lock`）的语义。

│   └── neo4j-community-4.4.34/       # 预置 Neo4j 图数据库

├── linux-6.6.1/                      # 待分析的 Linux 内核源码*   **全自动化流程**：编写了 `full_run.sh`，实现了从“清理 -> 编译 -> 分析 -> 导入数据库 -> 启动服务”的一键式操作。    *   **锁集分析**：维护当前路径持有的锁集合，检测未加锁的全局变量访问。### 4. 双重输出模式

├── analysis_data/                    # [生成] 静态分析产生的 JSON 数据

├── neo4j_data_linux-6.6.1/           # [生成] 图数据库导入文件 (nodes/edges)

├── full_run.sh                       # 一键全流程运行脚本

├── test_toolchain.sh                 # 单元测试脚本## 6. 系统测试情况*   **输出**：为每个编译单元生成 JSON 格式的分析报告。*   **简明分析**: 汇总日志 (`analysis.log`)，列出所有检测到的交互及潜在的竞争条件。

├── start_neo4j.sh                    # 数据库启动脚本

├── NEO4J_QUERIES.md                  # 可视化查询语句说明文档### 单元测试

├── README.md                         # 项目主文档 (本文件)

└── USAGE.md                          # 详细操作指南*   **测试用例**：`test/sample_kernel.c` 和 `test/viz_test.c`。*   **可视化 AST**: 详细日志 (`ast.log`)，显示每个函数的控制流图 (基本块) 和 GIMPLE 语句，并将分析结果嵌入上下文中。

```

*   **覆盖范围**：包含全局变量读写、函数指针调用、锁的获取与释放、死锁场景模拟。

## 10. 比赛收获

*   **深入理解编译器原理**：通过开发 GCC 插件，深入理解了编译器的中间表示（GIMPLE）、控制流图（CFG）和过程间分析（IPA）技术。*   **工具**：`test_toolchain.sh` 用于快速验证插件逻辑的正确性。### 3.2 数据处理层 (Python ETL)*   **竞争警告列表**: 自动提取生成的 `race_warnings.txt`，专注于安全审计。

*   **掌握内核构建体系**：熟悉了 Kbuild 系统，学会了如何在庞大的内核工程中嵌入自定义分析工具。

*   **图数据库应用实践**：掌握了将代码静态分析结果转化为图数据的思路，并熟练使用 Cypher 语言进行复杂代码模式的挖掘。

*   **团队协作能力**：在解决高难度技术问题的过程中，锻炼了团队分工、代码协作和文档管理的能力。

### 集成测试 (Linux Kernel v6.6.1)*   **数据清洗**：合并分散的 JSON 文件，去除重复数据。

*   **测试环境**：Ubuntu Linux, GCC 13, JDK 17, Neo4j 4.4.34。

*   **测试规模**：完整编译 Linux 6.6.1 内核，分析了数万个源文件。*   **格式转换**：将分析结果转换为图数据库所需的节点（Nodes）和边（Edges）格式（CSV）。### 5. Neo4j 数据库导出与可视化

*   **结果**：

    *   生成节点数：> 80,000 (函数与变量)*   **实体映射**：*   **大数据量支持**: 针对 Linux 内核级别的海量数据，自动生成 Neo4j 导入文件。

    *   生成边数：> 200,000 (调用与访问关系)

    *   成功检测出内核中已知的并发访问模式。    *   Node: Function, GlobalVariable, Lock*   **生成文件**: `neo4j_data_linux-6.6.1/` 目录下包含 `nodes.csv` 和 `edges.csv`。



详细的操作指南请参考 [USAGE.md](USAGE.md)。    *   Edge: CALLS, READS, WRITES, ACQUIRES, RELEASES*   **一键启动**: 提供 `full_run.sh` 脚本，自动完成分析、数据导入和数据库启动。



## 7. 遇到的主要问题和解决方法*   **可视化查询**: 详见 `NEO4J_QUERIES.md`，提供多种场景的 Cypher 查询语句。

### 问题 1：局部变量与全局变量同名导致误报

*   **现象**：简单的字符串匹配会将局部变量 `count` 误认为是全局变量 `count`。### 3.3 数据展示层 (Neo4j)

*   **解决**：放弃基于名称的匹配，转而检查 GCC 的 `TREE_STATIC` 和 `DECL_EXTERNAL` 标志，准确判断变量存储类型。

*   **存储**：使用 Neo4j 图数据库存储全量内核调用图。## 环境安装与配置

### 问题 2：大规模数据可视化崩溃

*   **现象**：最初尝试使用前端库（vis.js）和 Gephi 直接加载所有数据，导致浏览器崩溃或软件无响应。*   **查询**：通过 Cypher 查询语言提供灵活的检索（如“查找所有访问变量 X 且未加锁的路径”）。

*   **解决**：引入 Neo4j 图数据库。将数据存储在磁盘，按需查询（Cypher），只渲染用户关心的子图（Subgraph），解决了性能瓶颈。

### 1. 安装 GCC 插件开发环境

### 问题 3：环境配置复杂

*   **现象**：评委或协作者复现项目时，常因 JDK 版本、Neo4j 版本不一致导致运行失败。## 4. 开发计划本项目依赖 GCC 的插件机制，因此需要安装对应版本的 GCC 及其插件开发头文件。

*   **解决**：将 JDK 17 和 Neo4j Community 版直接集成到项目 `tools/` 目录中，并编写脚本自动配置环境变量，实现“开箱即用”。

1.  **环境搭建阶段**：配置 Linux 6.6.1 内核构建环境，熟悉 GCC 插件开发接口。

## 8. 分工和协作

2.  **原型验证阶段**：编写最小化插件，验证能否在内核编译过程中打印函数名和变量名。**Ubuntu/Debian:**

| 成员姓名 | 分工职责 |

| :--- | :--- |3.  **核心功能开发**：```bash

| **张乐桁** | 负责代码主要编码 |

| **潘利源** | 负责修改代码 |    *   实现 GIMPLE 语句遍历器。# 1. 安装基础构建工具

| **黄永康** | 负责撰写文档 |

    *   实现全局/局部变量区分逻辑。sudo apt update

## 9. 提交仓库目录和文件描述

```text    *   实现锁集分析算法（Lockset Analysis）。sudo apt install build-essential flex bison libssl-dev libelf-dev libgmp-dev libmpc-dev libmpfr-dev

.

├── src/4.  **数据流打通**：设计 JSON 输出格式，编写 Python 转换脚本，搭建 Neo4j 数据库。

│   └── plugin/analyzer_plugin.cpp    # GCC 插件核心源代码

├── tools/5.  **大规模测试与优化**：在完整内核上运行，解决内存溢出和性能瓶颈问题。# 2. 安装 GCC 及其插件开发包 (以 GCC 13 为例)

│   ├── export_to_neo4j.py            # 数据转换脚本 (JSON -> CSV)

│   ├── jdk-17.0.2/                   # 预置 Java 运行环境6.  **文档与可视化**：编写查询语句文档，优化图谱展示效果。sudo apt install gcc-13 g++-13 gcc-13-plugin-dev

│   └── neo4j-community-4.4.34/       # 预置 Neo4j 图数据库

├── linux-6.6.1/                      # 待分析的 Linux 内核源码

├── analysis_data/                    # [生成] 静态分析产生的 JSON 数据

├── neo4j_data_linux-6.6.1/           # [生成] 图数据库导入文件 (nodes/edges)## 5. 比赛过程中的重要进展# 3. 设置默认 GCC 版本 (可选)

├── full_run.sh                       # 一键全流程运行脚本

├── test_toolchain.sh                 # 单元测试脚本*   **突破变量遮蔽难题**：成功利用 GCC `varpool_node` 和 `tree` 结构，以 100% 准确率区分了同名局部变量和全局变量。sudo update-alternatives --install /usr/bin/gcc gcc /usr/bin/gcc-13 60

├── start_neo4j.sh                    # 数据库启动脚本

├── NEO4J_QUERIES.md                  # 可视化查询语句说明文档*   **实现过程间锁分析**：从最初的单函数分析，进化到支持跨函数调用的锁状态追踪，能够识别 `wrapper` 函数（如 `my_lock` 封装 `spin_lock`）的语义。sudo update-alternatives --install /usr/bin/g++ g++ /usr/bin/g++-13 60

├── README.md                         # 项目主文档 (本文件)

└── USAGE.md                          # 详细操作指南*   **全自动化流程**：编写了 `full_run.sh`，实现了从“清理 -> 编译 -> 分析 -> 导入数据库 -> 启动服务”的一键式操作。```

```



## 10. 比赛收获

*   **深入理解编译器原理**：通过开发 GCC 插件，深入理解了编译器的中间表示（GIMPLE）、控制流图（CFG）和过程间分析（IPA）技术。## 6. 系统测试情况### 2. 安装与配置 Neo4j

*   **掌握内核构建体系**：熟悉了 Kbuild 系统，学会了如何在庞大的内核工程中嵌入自定义分析工具。

*   **图数据库应用实践**：掌握了将代码静态分析结果转化为图数据的思路，并熟练使用 Cypher 语言进行复杂代码模式的挖掘。### 单元测试本项目使用 Neo4j 图数据库进行可视化。你可以使用项目 `tools/` 目录下预置的版本，也可以自行安装。

*   **团队协作能力**：在解决高难度技术问题的过程中，锻炼了团队分工、代码协作和文档管理的能力。

*   **测试用例**：`test/sample_kernel.c` 和 `test/viz_test.c`。

*   **覆盖范围**：包含全局变量读写、函数指针调用、锁的获取与释放、死锁场景模拟。**方案 A：使用项目预置版本 (推荐)**

*   **工具**：`test_toolchain.sh` 用于快速验证插件逻辑的正确性。本项目已在 `tools/` 目录下集成了 Neo4j Community Edition 4.4.34 和 JDK 17。

*   无需额外安装，直接运行 `./full_run.sh` 或 `./start_neo4j.sh` 即可。

### 集成测试 (Linux Kernel v6.6.1)

*   **测试环境**：Ubuntu Linux, GCC 13, JDK 17, Neo4j 4.4.34。**方案 B：自行手动安装**

*   **测试规模**：完整编译 Linux 6.6.1 内核，分析了数万个源文件。如果你需要重新部署环境：

*   **结果**：1.  **下载 Neo4j**:

    *   生成节点数：> 80,000 (函数与变量)    *   访问 [Neo4j 下载中心](https://neo4j.com/download-center/#community) 下载 Community Edition (推荐 4.4.x 或 5.x)。

    *   生成边数：> 200,000 (调用与访问关系)    *   解压到 `tools/` 目录。

    *   成功检测出内核中已知的并发访问模式。2.  **配置 Java 环境**:

    *   Neo4j 4.x 需要 Java 11，Neo4j 5.x 需要 Java 17。

## 7. 遇到的主要问题和解决方法    *   确保 `JAVA_HOME` 环境变量指向正确的 JDK 路径。

### 问题 1：局部变量与全局变量同名导致误报3.  **配置远程访问 (可选)**:

*   **现象**：简单的字符串匹配会将局部变量 `count` 误认为是全局变量 `count`。    *   修改 `conf/neo4j.conf`:

*   **解决**：放弃基于名称的匹配，转而检查 GCC 的 `TREE_STATIC` 和 `DECL_EXTERNAL` 标志，准确判断变量存储类型。        ```properties

        # 允许远程连接

### 问题 2：大规模数据可视化崩溃        dbms.default_listen_address=0.0.0.0

*   **现象**：最初尝试使用前端库（vis.js）和 Gephi 直接加载所有数据，导致浏览器崩溃或软件无响应。        # 禁用鉴权 (仅限测试环境)

*   **解决**：引入 Neo4j 图数据库。将数据存储在磁盘，按需查询（Cypher），只渲染用户关心的子图（Subgraph），解决了性能瓶颈。        dbms.security.auth_enabled=false

        ```

### 问题 3：环境配置复杂

*   **现象**：评委或协作者复现项目时，常因 JDK 版本、Neo4j 版本不一致导致运行失败。## 目录结构

*   **解决**：将 JDK 17 和 Neo4j Community 版直接集成到项目 `tools/` 目录中，并编写脚本自动配置环境变量，实现“开箱即用”。*   `src/plugin/`: GCC 插件的源代码 (`analyzer_plugin.cpp`)。

*   `linux-6.6.1/`: 目标 Linux 内核源代码。

## 8. 分工和协作*   `build_analysis_linux-6.6.1/`: 树外内核构建目录 (保持源码整洁)。

*   **张乐桁**：负责代码主要编码。包括 GCC 插件核心逻辑实现（GIMPLE 遍历、锁集分析）、构建系统集成。*   `analysis_data/`: 存放 GCC 插件生成的原始 JSON 分析数据。

*   **潘利源**：负责修改代码。包括 Bug 修复、代码优化、适配不同内核版本、测试用例编写。*   `neo4j_data_linux-6.6.1/`: 存放转换后的 CSV 导入文件。

*   **黄永康**：负责撰写文档。包括项目 README、设计文档、Neo4j 查询语句编写及演示材料制作。*   `tools/`: 包含 Neo4j 数据库、Java 环境及辅助脚本。

*   `full_run.sh`: **[推荐]** 一键重构并运行全流程的脚本。

## 9. 提交仓库目录和文件描述*   `start_neo4j.sh`: 单独启动 Neo4j 服务的脚本。

```text*   `test_toolchain.sh`: 测试工具链的脚本。

.

├── src/## 常用 Neo4j 查询

│   └── plugin/analyzer_plugin.cpp    # GCC 插件核心源代码详细查询语句请参考 [NEO4J_QUERIES.md](NEO4J_QUERIES.md)。

├── tools/

│   ├── export_to_neo4j.py            # 数据转换脚本 (JSON -> CSV)**快速开始：查看某个变量的调用链**

│   ├── jdk-17.0.2/                   # 预置 Java 运行环境```cypher

│   └── neo4j-community-4.4.34/       # 预置 Neo4j 图数据库MATCH path = (start:Function)-[:CALLS*1..5]->(end:Function)-[:READS|WRITES]->(g:GlobalVariable {name: 'vc_class'})

├── linux-6.6.1/                      # 待分析的 Linux 内核源码RETURN path

├── analysis_data/                    # [生成] 静态分析产生的 JSON 数据LIMIT 50

├── neo4j_data_linux-6.6.1/           # [生成] 图数据库导入文件 (nodes/edges)```

├── full_run.sh                       # 一键全流程运行脚本
├── test_toolchain.sh                 # 单元测试脚本
├── start_neo4j.sh                    # 数据库启动脚本
├── NEO4J_QUERIES.md                  # 可视化查询语句说明文档
├── README.md                         # 项目主文档
└── USAGE.md                          # 详细操作指南
```

## 10. 比赛收获
*   **深入理解编译器原理**：通过开发 GCC 插件，深入理解了编译器的中间表示（GIMPLE）、控制流图（CFG）和过程间分析（IPA）技术。
*   **掌握内核构建体系**：熟悉了 Kbuild 系统，学会了如何在庞大的内核工程中嵌入自定义分析工具。
*   **图数据库应用实践**：掌握了将代码静态分析结果转化为图数据的思路，并熟练使用 Cypher 语言进行复杂代码模式的挖掘。
*   **团队协作能力**：在解决高难度技术问题的过程中，锻炼了团队分工、代码协作和文档管理的能力。
